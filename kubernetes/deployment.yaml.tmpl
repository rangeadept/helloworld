apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: helloworld

spec:
  replicas: 1
  template:
    metadata:
      name: helloworld
      labels:
        app: helloworld
        track: stable

    spec:
      containers:
        - name: helloworld
          image: gcr.io/$PROJECT_NAME/$APP_NAME:${CIRCLE_SHA1}
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /view/health
              port: 8080
